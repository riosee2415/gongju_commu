doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        a(href="/board/create")
            button 글작성
        br
        br
        each board in boardList
            a(href="/board/list/" + board.id)
                #{board.id} | #{board.title} | #{board.createdAt} | #{board.UserId}
            br
            hr